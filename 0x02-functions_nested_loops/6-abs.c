#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @c: The integer to be computed
 * Return: The absolute value
 */

int _abs(void)
{
	if (t >= 0)
	{
		return (t);
	}
	else
	{
		return (-t);
	}
}
